/* Begin, prompt: Generate the data models... Messages with status (sent, delivered, read). */
package com.example.whichzup.chat.domain.model

enum class MessageStatus {
    SENDING,
    SENT,
    DELIVERED,
    READ
}
/* End */